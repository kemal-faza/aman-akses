# Daily Notes — 2026-06-12

## Tasks Completed

### Fix final code review MEDIUM issues (Jurnal Aman feature)

**Files modified:**
- `components/ui/alert-dialog.tsx` — NEW: Created AlertDialog component using `@base-ui/react/alert-dialog` (not Radix UI), following existing Sheet component pattern
- `app/(main)/_components/journal/MoodTracker.tsx` — Added arrow key navigation (ArrowLeft/Right/Up/Down) with `onKeyDown` on the radiogroup container
- `app/(main)/jurnal/page.tsx` — Replaced `window.confirm()` with accessible AlertDialog, added `refreshEntries` and `onRetry` prop, fixed unhandled promise in delete
- `app/(main)/_components/journal/JournalList.tsx` — Added `onRetry` prop with "Coba Lagi" button in error state, added `useMemo` for sorted entries

**Key technical decisions:**
- AlertDialog built with `@base-ui/react/alert-dialog` (project uses `@base-ui/react` instead of Radix UI for all primitives)
- Delete confirmation uses controlled `open` prop with `deleteTarget` state — onOpenChange guarded with `!open` check to prevent race condition on dialog open
- `confirmDelete` async with try/catch/finally to handle unhandled promise rejection
- Sort memoized with `useMemo([entries])` to avoid re-sorting on every render

**Verification:**
- `npx tsc --noEmit` — 1 pre-existing globals.css error (unrelated)
- `npx vitest run` — 83 passed, 0 failed
